class program {


program() {
  while(nextToABeeper) {
    while(nextToABeeper) {
      pickbeeper();
      move();
    }
    turnleft();
    iterate(2) {
      turnleft();
      move();
    }
  }
  turnoff();
}

}
